{% extends 'base.html.twig' %}

{% block body %}
    <h1>Equipes list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Entraineurid</th>
                <th>Entraineurnomclub</th>
                <th>Championnat</th>
                <th>Categorie</th>
                <th>Numequipe</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for equipe in equipes %}
            <tr>
                <td><a href="{{ path('equipes_show', { 'id': equipe.id }) }}">{{ equipe.id }}</a></td>
                <td>{{ equipe.entraineurId }}</td>
                <td>{{ equipe.entraineurNomClub }}</td>
                <td>{{ equipe.championnat }}</td>
                <td>{{ equipe.categorie }}</td>
                <td>{{ equipe.numEquipe }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('equipes_show', { 'id': equipe.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('equipes_edit', { 'id': equipe.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('equipes_new') }}">Create a new equipe</a>
        </li>
    </ul>
{% endblock %}
