{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('build/admin_equipes.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h1>CRÉATION ÉQUIPE :</h1>
        <div class="jumbotron" style="color:black;">
            {{ form_start(form, {'method': 'post', 'action': path('equipes_new')}) }}
            {#<div class="form-group input-lg col-lg-12">
                #}{#<div class="row">
                    <label for="{{ form.entraineurId.vars.id }}" class="required">Id</label>
                </div>
                <div class="row">
                    <input type="number" id="{{ form.entraineurId.vars.id }}" name="{{ form.entraineurId.vars.full_name }}" required="required" value="{{ userId }}"/>
                </div>#}{#
                <div class="row">
                    {{ form_label(form.entraineurId) }}
                </div>
                <div class="row erreurs text-center">
                    {{ form_errors(form.entraineurId) }}
                </div>
                <div class="row">
                    {{ form_widget(form.entraineurId) }}
                </div>
            </div>#}
            <div class="form-group input-lg col-lg-12">
                <div class="row">
                    <label for="nom_entraineur">Nom de l'entraîneur</label>
                </div>
                <div class="row">
                    <div class="form-control">
                        {{ nom }} {{ prenom }}
                    </div>
                </div>
            </div>
            <div class="form-group input-lg col-lg-12">
                <div class="row">
                    {#{{ form_label(form.entraineurNomClub, null, {'label': 'Nom du club'}) }}#}
                    <label for="">Nom du club</label>
                </div>
                <div class="row">
                    {#<input type="text" id="{{ form.entraineurNomClub.vars.id }}" name="{{ form.entraineurNomClub.vars.full_name }}" required="required"  value="{{ club }}"/>#}
                    <div class="form-control">
                        {{ club }}
                    </div>
                </div>
            </div>
            <div class="form-group input-lg col-lg-12">
                <div class="row">
                    {{ form_label(form.championnat) }}
                </div>
                <div class="row erreurs text-center">
                    {{ form_errors(form.championnat) }}
                </div>
                <div class="row">
                    {{ form_widget(form.championnat) }}
                </div>
            </div>
            <div class="form-group input-lg col-lg-12">
                <div class="row">
                    {{ form_label(form.categorie, null, {'label': 'Catégorie'}) }}
                </div>
                <div class="row erreurs text-center">
                    {{ form_errors(form.categorie) }}
                </div>
                <div class="row">
                    {{ form_widget(form.categorie) }}
                </div>
            </div>
            <div class="form-group input-lg col-lg-12">
                <div class="row">
                    {{ form_label(form.numEquipe, null, {'label': "Numéro d'équipe"}) }}
                </div>
                <div class="row erreurs text-center">
                    {{ form_errors(form.numEquipe) }}
                </div>
                <div class="row">
                    {{ form_widget(form.numEquipe) }}
                </div>
            </div>
            <br>
            <div class="text-right">
                <button class="btn-style btn-align btn" type="submit" value="Create">CRÉER</button>
                {{ form_end(form) }}
                <br>
                <a href="{{ path('equipes_index') }}">
                    <button class="btn-style btn-align btn">RETOUR À LA LISTE</button>
                </a>
            </div>
        </div>
    </div>
{% endblock %}
